import React from "react";
import { ImageModalInterface } from "../type";

export function ImageModalView(props: ImageModalInterface) {
  //   const { content, typography, color } = options;

  return <div></div>;
}
